
from django.urls import path, re_path, include

from rest_framework import routers

# from .views import home

urlpatterns = [
    # path(r'sendmail/', sendmail, name='send-mail'),
    # path(r'homepage/', home, name='home-page'),
]
